apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "akeyless-services.fullname" . }}-test-connection"
  labels:
    {{- include "akeyless-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "akeyless-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
